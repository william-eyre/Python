my_tuples = (1, 2, 3, 4, 5)
print(my_tuples)
# a for loop can iterate over the elements in a tuple
names = ('Holly', 'Warren', 'Ashley')
for n in names:
    print(n)
# tuples support indexing
names = ('Holly', 'Warren', 'Ashley')
for i in range(len(names)):
    print(names[i])
    # create a tuple with just one element
    my_tuples = (1,)  # Creates a tuple with one element.

# a tuple is a constant array, if you want your array to always be a set of figures then use tuple.

